# Custom Configurations

Place your project-specific customizations here.

Files in this directory override default configurations.

Examples:
- `custom-severity-matrix.csv` — Override default SEV1-4 definitions
- `custom-prr-dimensions.csv` — Add/modify PRR dimensions
- `custom-team.yaml` — Define your team structure
